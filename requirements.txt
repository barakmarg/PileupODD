# Runtime dependencies for the preprocessing pipeline.
#
# Deliberately short. Pruning primary/preprocessing.py to its reachable
# functions dropped torch, fastjet, awkward and sklearn.cluster; leaving
# train/val/test splitting to the dataloader dropped the last scikit-learn
# use (train_test_split). All four were carried by the master branch.
polars>=1.20
numpy>=1.26
pyarrow>=15.0            # parquet IO behind polars
PyYAML>=6.0
tqdm>=4.66
datasketches>=5.0        # KLL sketches for normalization quantiles
psutil>=5.9              # RAM reporting in progress output

# CLUEstering is deliberately NOT listed here. It is a C++/CUDA extension whose
# GPU backend is only compiled if nvcc is visible at configure time, so
# installing it implicitly from an index can silently leave you with CPU-only
# backends -- and every `backend: gpu cuda` run then fails at clustering time.
# Build it from source and verify the backend list: see "Install" in the README.
